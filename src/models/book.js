export default class Book {
    constructor(title, author, filepath, conver_url, type) {
        this.title = title
        this.author = author
        this.filepath = filepath
        this.conver_url = conver_url
        this.type = type
    }
}